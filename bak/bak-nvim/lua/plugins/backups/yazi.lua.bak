return {
    "mikavilpas/yazi.nvim",
    event = "VeryLazy",
    keys = {

    },
    opts = {
        open_for_directories = true, -- replaces the netrw with yazi, useful when opening folder with neovim
    },
}
